<%= form_for @group do |f|%>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<hr>
	<%= f.select(:sport_id, @sports.collect {|sport| [sport.title, sport.id]}) %>
	<hr>
	<%= f.submit 'Create' %>
	<hr>
<%end%>
<br>
<%= link_to("Back", '/', style: "btn btn-default")%>

