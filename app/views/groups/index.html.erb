<h1>Yea! this is gonna be groups page!</h1>

<% @group.each do |group|%>
	<h3><%= link_to(group.name, group_path(group))%></h3>
	<%end%>

<button><%= link_to("Add new group", new_group_path)%></button>