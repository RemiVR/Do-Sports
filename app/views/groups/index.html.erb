<h1>Yea! this is gonna be groups page!</h1>

<% @group.each do |group|%>
	<h2><%= link_to(group.name, group_path(group))%></h2>
	<%end%>

<button type="button" class="btn btn-default"><%= link_to("Add new group", new_group_path)%></button>