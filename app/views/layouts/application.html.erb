<!DOCTYPE html>
<html>
<head>
  <title>DoSports</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => false %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => false %>
  <%= csrf_meta_tags %>
</head>
<body>
<header>
	<div class="container my-header">
		<div class="row">
			<div class="col-md-12">
			<a href="/"><img src="/assets/finish_logo.png" height="160px"></a>
			</div>
			<% if current_user %>
				<div class="col-md-3 pull-right">
		  		Signed in as <%= link_to(image_tag(current_user.photo.url(:thumb)), user_path(current_user.id))%>  <%= link_to "Logout", '/logout', :class => "btn btn-danger" %>
		  	</div>
			<%else%>
			<div class="col-md-6 pull-right">
				<div class="form-inline">
		  		<div class="form-group">
						<%= flash[:error]%>
						<%= form_tag login_path do %>
							<%= email_field_tag :email, nil, :class => "form-control", placeholder: "Email" %>
							<%= password_field_tag :password, nil, :class => "form-control", placeholder: "Password" %>
							<%= submit_tag "Sign in", :class => "btn btn-success" %>
							<%= link_to 'Signup', '/signup' %>
						<% end %>
					</div>
				</div>
			</div>
		</div>
		<%end%>
			<div class="col-md-5 pull-left">
				<ul class="nav nav-tabs">
				  <li role="presentation"><a href="/">Home</a></li>
				  <li role="presentation"><a href="user_path(current_user.id">My Group</a></li>
				  <li role="presentation"><a href="#">Profile</a></li>
				  <li role="presentation"><a href="#">About us</a></li>
				</ul>
			</div>
			</form>
		</div>
	</div>
</header>
	<div class="container">
		<%= yield %>
	</div>
<footer>
	<div class="">
			
		</div>
	</div>
</footer>

</body>
</html>