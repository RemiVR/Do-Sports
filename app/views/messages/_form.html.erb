<%= form_for([@group, @group.messages.build]) do |f|%>
	<%= f.hidden_field :user_id, value: current_user.id %>
	<%= f.text_field :content, :class => "form-control" %>
	<%= f.submit "submit", :class => "btn btn-default form-group-btn"%>
<% end %>