<h3>User Profile</h3>
<ul>
	<%= flash[:notice]%>
	<li><%= @user.name%></li>
	<li><%= @user.email%></li>
</ul>
<% unless current_user.nil? || @user.id != current_user.id%>
<%= link_to("Edit", edit_user_path)%>
<%end%>
